{
  "name": "@repo/nodejs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "biome lint ./ && tsc --noEmit",
    "format": "biome check ./ --apply"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^20.12.7",
    "typescript": "^5.8.3"
  }
}
