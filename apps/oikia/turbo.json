{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "config/**", "schema/**"],
      "outputs": ["build/**"]
    },
    "start": {
      "persistent": true,
      "cache": false
    }
  }
}
